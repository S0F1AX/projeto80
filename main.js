nomeConvidados=[];
var nomeConvidados = document.getElementById("NomeConvidados1").value;
NomeConvidados.push(nomeConvidados);
NomeConvidados.sort();

function searching()
{
    var s= document.getElementById("s1").value;
    var found=0;
    var i:
    for(i=0; i<nomeConvidados.legth; i++)
    document.getElementById(p2).innerHTML="Nome encontrado"+found+"vez(es)";
    console.log("Nome encontrado"+found+"vez(es)");
}